
  # Product Requirements Document

  This is a code bundle for Product Requirements Document. The original project is available at https://www.figma.com/design/Ya7W5QEI3zsx0rR8KMlDst/Product-Requirements-Document.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  